# Amazon Inspector

Floci implements the REST JSON Inspector organization operations used by local security-governance workflows.

Supported behavior includes delegated administrator management, account status lookup, enablement, and organization auto-enable configuration.

State is persisted through the configured Floci storage backend.
