# Identity Store

Floci supports the JSON 1.1 Identity Store operations needed for local IAM Identity Center provisioning.

## Supported operations

`ListGroups`, `CreateGroup`, `ListUsers`, `CreateUser`, `IsMemberInGroups`, and `CreateGroupMembership`.

Groups, users, and memberships are stored through the configured Floci storage backend.
