# Amazon Detective

Floci implements the REST JSON Detective organization and graph operations used by local security-governance workflows.

Supported behavior includes administrator management, graph listing, organization configuration, member creation/listing, and member monitoring state.

State is persisted through the configured Floci storage backend.
