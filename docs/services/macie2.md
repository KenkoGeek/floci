# Amazon Macie

Floci implements the REST JSON organization surfaces used to configure Macie locally.

Supported behavior includes delegated administrator listing and enablement, Macie session lookup and enablement, and organization auto-enable configuration.

State is persisted through the configured Floci storage backend.
