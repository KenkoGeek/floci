# AWS Account Management

Floci supports alternate-contact management over the AWS REST JSON protocol.

## Supported operations

- `PutAlternateContact`
- `GetAlternateContact`

Alternate contacts are isolated by caller account and stored through the configured Floci storage backend.
